machine:
  services:
    - docker

test:
  override:
    - make test
    - make clean

compile:
  override:
    - make test
    - make clean

deployment:
  release:
    tag: /v[0-9]+(\.[0-9]+)*/
    commands:
      - make ci-build